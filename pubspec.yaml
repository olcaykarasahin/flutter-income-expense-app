name: gelir_gider
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ===== ISAR (Runtime) =====
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1

  # ===== REQUIRED FOR ISAR PATH =====
  path_provider: ^2.1.4
  provider: ^6.1.5+1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # ===== ISAR CODE GENERATION =====
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
